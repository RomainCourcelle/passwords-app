<!DOCTYPE html>
<html lang=fr>

<head>
  <meta charset="utf-8">
  <title>{{ title || "Passwords" }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  @vite(['resources/css/app.css', 'resources/js/app.js'])
  {{{ await $slots.meta() }}}
</head>

<body class="min-h-screen flex flex-col bg-gradient-to-b from-slate-700 to-slate-900 text-white">
  <nav class="w-full p-4 border-b-2 border-gray-400 flex items-center justify-between">
    <a href="/" class="text-2xl font-bold">Passwords</a>
    @if( auth.user )
    <form method="POST" action="/logout">
      {{ csrfField() }}
      <button type="submit"
        class="cursor-pointer font-semibold bg-red-700 hover:bg-red-500 rounded-lg p-2">Déconnexion</button>
    </form>

    @else
    <a href="/login" class="font-semibold bg-sky-700 hover:bg-sky-500 rounded-lg p-2">Connexion</a>
    @endif
  </nav>
  {{{ await $slots.main() }}}
</body>

</html>